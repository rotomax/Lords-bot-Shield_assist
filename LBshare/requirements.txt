# LordsBot — Python dependencies
# Install:  pip install -r requirements.txt

pyautogui>=0.9.54
opencv-python>=4.5.0
numpy>=1.21.0
pytesseract>=0.3.8
Pillow>=8.0.0
psutil>=5.8.0
pygetwindow>=0.0.9       # Windows — for focusing game window
keyboard>=0.13.5         # global stop-hotkey (optional but recommended)
